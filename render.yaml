services:
  - type: web
    name: indoor-wellness-ai
    env: docker
    plan: free
    region: oregon
    healthCheckPath: /api/health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: indoor-wellness-db
          property: connectionString

